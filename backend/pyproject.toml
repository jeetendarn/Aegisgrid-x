name = "aegisgrid-x-backend"
version = "0.1.0"
description = "AegisGrid X backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
    "ruff",
]
EOF